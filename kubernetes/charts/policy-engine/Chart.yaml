apiVersion: v2
name: policy-engine
description: A Helm chart for Kyverno Policy Engine and admission control rules
type: application
version: 0.2.0

dependencies:
  - name: kyverno
    version: "3.8.2"
    repository: "https://kyverno.github.io/kyverno/"
    condition: kyverno.enabled
